# Experiments

Experiment configurations define benchmark, provider/model, prompt protocol, and output settings. Runtime artifacts are written under `experiments/<experiment_id>/runs/<run_id>/` and may become large, so run directories are ignored by Git.

Small curated aggregate results and provenance manifests can be committed at the experiment level when they are reproducible, reviewable, and permitted by upstream licenses. Failed runs remain retained as observations.
